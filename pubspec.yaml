name: morning_routine
description: A production-ready Flutter app helping users build perfect morning routines.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  audioplayers: ^1.2.0
  image_picker: ^1.0.5
  shared_preferences: ^2.2.2
  flutter_hooks: ^0.18.6
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
